<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xsx.account">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddItemActivity" />
        <activity android:name=".ui.AccountingActivity" />
        <activity android:name=".ui.PipeChartActivity" />
        <activity android:name=".ui.LineChartActivity" />
        <activity android:name=".ui.PreviewColumnChartActivity" />
        <activity android:name=".ui.BillDetailActivity"></activity>
    </application>

</manifest>
